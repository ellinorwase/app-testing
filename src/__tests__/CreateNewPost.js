import CreateNewPost from '../components/CreateNewComment';

// beforeEach(() => {
//     jest.resetModules();
//   });

//   test('returns all posts', () => {
//     const todoList = require('../src/Posts');
//     expect(todoList.getAllTodos()).toHaveLength(3);
//   });