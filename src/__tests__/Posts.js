beforeEach(() => {
//     jest.resetModules();
//   });

//   test('returns all posts', () => {
//     const posts = require('../src/Posts');
//     expect(posts.removePost()).toHaveLength(1);
//   });